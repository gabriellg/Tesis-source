// La tabla de las probabilidades.

class CProbabilityTable
{
    public:
    
    	CProbabilityTable();
    	virtual ~CProbabilityTable();
        
        bool positionTreeGrowning(unsigned long *row, unsigned long *col);
        bool positionStorm(unsigned long *row, unsigned long *col);
        
        void setEmptyTree(unsigned long row, unsigned long col);
        
    private:
    
        struct prv_dataPrivate_t *m_dataPrivate;
};
