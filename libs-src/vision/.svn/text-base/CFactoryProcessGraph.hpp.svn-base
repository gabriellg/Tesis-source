// Factoria que crea procesos de imagen.

class CFactoryProcessGraph
{
	public:
	
		CFactoryProcessGraph() {;}
		virtual ~CFactoryProcessGraph() {;}
		
		virtual class CProcessGraphImages *processGraphImages() const = 0;
};
